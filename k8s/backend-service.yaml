apiVersion: v1
kind: Service

metadata:
  name: backend-service

spec:
  type: NodePort

  selector:
    app: artstore-backend

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007